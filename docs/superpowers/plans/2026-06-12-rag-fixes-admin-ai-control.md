# RAG Fixes + Admin AI Control + Data Management — Spec & Plan

> SDD execution. Mobile fixes are JS-only → OTA 1.6.0. Admin ships via Vercel push. TDD. Never `{count && <JSX/>}`. Schema rule (mobile sqlite): NOT NULL columns need .notNull().default(). New user_settings-adjacent tables → update test fixtures.

## Verified diagnosis (prod screenshots + code + DB)
- Gemini RAG IS flowing (knew name/BUCET/60%) but: (1) buildListingsContext NEVER includes `external_url` (not selected, not formatted) and (2) CORE_RULES hardcodes "verify at upcat.up.edu.ph" → model fabricates UPCAT URLs for ANY exam. Prod listings.external_url values are present and plausible (BUCET → bicol-u.edu.ph — correct).
- Local empty context: (a) STATS RACE — useKuyaChat captures useHomeStats() which is DEFAULT (listing:null) until its first load; sending early → progress block = "No focused exam yet" though one exists; (b) topic addendum "If unsure: check your textbook" makes the 1B refuse when retrieval misses.
- Admin console has NO AI-config surface (no table exists), NO career/schools/admissions CRUD; listings drawer missing scholarship_meta + results_date UI.

## Task A — Mobile RAG/prompt fixes (TDD)
Files: services/chatContext.ts, services/chatPrompts.ts, hooks/useKuyaChat.ts, services/flashcardRetriever.ts (tests), services/ragPipeline.ts (none/minor).
1. buildListingsContext: SELECT + format `externalUrl` → line format gains ` · official site: <url>` when non-empty. cachedQuery key bump ('chat:listings-meta-v2') so stale caches refresh.
2. CORE_RULES: REMOVE the hardcoded upcat.up.edu.ph line. REPLACE with URL rule: "Only mention a website if its URL appears in the context blocks. If the student needs an official site that isn't in context, tell them to open the exam's page in the Exams tab. Never construct or guess URLs." Keep no-guarantee rule generic ("verify with the school's official site").
3. Stats race: in useKuyaChat.send(), if `stats.listing === null` AND mode==='progress', re-pull fresh stats before building context: call the cached loader directly — simplest correct: import getSettings+the home cachedQuery? CLEANEST: useHomeStats already exposes refresh(); await `stats.refresh()` once when stats.listing===null, then read the hook's latest via a ref mirror (capture stats in a ref updated by effect; after refresh, read ref). Implementer designs minimal-correct; test: send with default stats → progress context contains real listing after refresh (mock).
4. Topic addendum: change "If unsure: 'I'm not sure — check your textbook.'" → "If the context blocks answer it, use them. If you genuinely don't know and context doesn't help, say so briefly and suggest checking the Review tab." (keeps honesty, stops eager refusal).
5. Retrieval test additions: mixed Tagalog/English question matches English cards (already passes — lock w/ test); listings URL inclusion test; prompt URL-rule tests (both providers).

## Task B — Remote AI config (Supabase + admin + mobile)
1. Supabase migration `033_ai_chat_config.sql`: table `ai_chat_config (id int primary key default 1 CHECK (id=1), core_rules_override text NOT NULL DEFAULT '', scope_block_override text NOT NULL DEFAULT '', grounding_rule_override text NOT NULL DEFAULT '', anti_injection_override text NOT NULL DEFAULT '', progress_addendum_override text NOT NULL DEFAULT '', topic_addendum_override text NOT NULL DEFAULT '', math_addendum_override text NOT NULL DEFAULT '', rag_total_token_budget int NOT NULL DEFAULT 700, rag_per_block_char_cap int NOT NULL DEFAULT 280, rag_blocks_enabled jsonb NOT NULL DEFAULT '{"flashcards":true,"listings":true,"courses":true,"progress":true}', updated_at timestamptz NOT NULL DEFAULT now())` + seed row 1 + RLS public-read/admin-write + updated_at trigger (same pattern as 031). Apply to prod.
2. Mobile: schema/client mirror table `ai_chat_config` (+ MIGRATIONS), sync pull (incremental gt updated_at, single row upsert). `services/aiConfig.ts`: `getAiConfig(db)` cachedQuery('chat:ai-config', 300s) → typed config with EMPTY-STRING-MEANS-DEFAULT semantics (override used only when non-empty; budgets used when >0; blocks_enabled parsed safely). chatPrompts: assembly functions accept optional overrides — `composeSystemPrompt(mode, cfg?)` built from (override || builtin) pieces; ragPipeline reads budgets + enabled-blocks from cfg. useKuyaChat fetches cfg alongside rag (Promise.all) and passes through. Fallback: cfg unavailable → builtins (NEVER block chat on config). Tests: override substitution, empty=default, budgets respected, disabled block skipped.
3. Admin: `/admin/ai-config` page (model: BlueprintEditor pattern) + API route (GET/PUT, requireAdmin, same auth pattern as exam-blueprints): textareas for each override (placeholder shows the CURRENT BUILT-IN text — ship the builtin strings to admin via the GET response so admins see defaults; a "reset to default" clears to ''), numeric inputs for budgets, toggles for blocks. Sidebar nav entry "AI Chat Config". Vitest: route auth + upsert; builtin-defaults endpoint shape.
4. SAFETY: overrides are appended-trust — the ANTI_INJECTION + SCOPE built-ins are ALWAYS included even when core_rules_override is set (compose = override-or-builtin per piece, but scope+anti-injection pieces are mandatory: override replaces TEXT but a non-empty override for those two still gets appended AFTER with the builtin if the override lacks the key phrases? KEEP SIMPLE: those two pieces are override-able like the rest — admins are trusted; document in the admin UI: "Removing guardrails affects all students immediately."). Mobile composes; log '[ai-config] overrides active: <names>' in dev.

## Task C — URL verification + prod data (controller-led, small)
1. Verify the 11 exam external_urls (WebSearch/HEAD): are they the current official admission pages (e.g. ateneo.edu/osa/acet still live? bicol-u.edu.ph/admissions.php? iapply.ust.edu.ph?). Also `SELECT slug,title,external_url FROM listings WHERE type='scholarship' AND external_url <> ''` — spot-verify the verified:true ones. Update stale URLs via SQL UPDATE (record before/after in the report). Mobile devices pick changes via incremental sync.
2. Admin ListingDrawer already edits external_url (verified) — no change.

## Task D — Admin listings completeness + admissions updates CRUD
1. ListingDrawer: add `results_date` field (date input → epoch ms? check the column type + how exam_date is handled — mirror it) and a **Scholarship Meta** collapsible section editing the known scholarship_meta JSONB keys as structured fields (read mobile's consumption: huc_excluded bool, target_year_levels string[], other_benefits string[] — grep mobile usages for the full key list) + raw-JSON fallback textarea (validated). PATCH route already passes fields through? Verify /api/admin/listings PATCH allows these columns; extend if filtered.
2. NEW `/admin/updates` (admissions updates CRUD): table list (severity badge, school, title, event_date) + drawer create/edit/delete → admissions_updates (id text gen? check mobile-consumed columns: report_date, severity urgent/important/info, school_slug, school_name, title, body, action_required, event_date, event_type, sources jsonb, verified bool). API route admin-gated. Sidebar entry "Admissions Updates". Vitest route tests.

## Task E — Career & Schools data managers (the long tail, ONE reusable editor)
Build `components/admin/DataTableManager.tsx`: generic list+drawer CRUD driven by a per-table column config (name, type text/number/bool/json/select, required) hitting a generic admin API route `/api/admin/data/[table]` (GET paginated/POST/PATCH/DELETE) with a STRICT allowlist of tables+columns server-side (NEVER raw table from client without allowlist). Tables wired (one nav section "Data Manager"): career_courses, career_facts, ai_career_impact, career_destinations, career_countries, career_programs, tertiary_schools, university_profiles, course_school_rankings, course_school_quality, bar_results, upcat_cutoffs, upcat_facts. Per-table configs in one file with the REAL column lists (read supabase migrations/mobile sync selects for authoritative columns). Search box (ilike on 1-2 key columns per config), pagination 50/page. Vitest: allowlist enforcement (unknown table/column → 400), auth, CRUD happy paths on one representative table mock.

## Ship order & gates
A (mobile, OTA-critical) → B (config end-to-end) → C (controller) → D → E. Each: implementer → combined spec+quality review → fixes. Final: mobile full jest+tsc, admin vitest+tsc, push (Vercel auto-deploys admin + web app), OTA for the mobile pieces. On-device: BUCET question cites bicol-u.edu.ph or no URL (never upcat.up.edu.ph); progress answers work immediately after opening chat; admin can edit prompts/guardrails and see them reflected on device after sync.
