/**
 * useGoogleOneTap (native stub)
 *
 * On native (iOS/Android) Google One Tap via GIS script is impossible.
 * This stub ensures the hook can be imported without errors on native;
 * the platform-specific `.web.ts` file handles the real implementation.
 */
export function useGoogleOneTap(): void {
  // No-op on native — One Tap is web-only via the .web.ts platform module.
}
